name: Generate Pacman Contribution Graph

on:
  schedule:
    - cron: "0 */12 * * *"   # every 12 hours
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write   # allow pushing back to repo

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Generate contribution graph
        uses: yoshi389111/github-profile-3d-contrib@0.7.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Push to output branch
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -B output
          mv ./out/pacman-contribution-graph.svg .
          mv ./out/pacman-contribution-graph-dark.svg .
          git add pacman-contribution-graph*.svg
          git commit -m "Update Pacman contribution graph"
          git push origin output --force
